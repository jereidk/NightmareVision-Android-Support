# Content
content/ignore_*/*
content/avas_evil_secrets/*

# Haxelib & HXPKG
.haxelib/*
hmm.json

### Mac
.DS_Store
.icloud

### Python (byte-compiled scripts, e.g. from py_compile checks on .github/scripts)
__pycache__/
*.pyc

### VS Code
vscode-project.hxml
export/*
.vscode/*
*.code-workspace
dump/*

# Local history which shouldn't be shared.
.history
.ionide

# ! means a file should be added regardless of it it gets ignored prior.
# Including to allow others to use already set-up configuration tweaked for the project.
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
/content
!content/NMV-Base-Game

# Claude Code worktrees
.claude/worktrees/
