# Content
content/ignore_*/*
content/avas_evil_secrets/*

# Haxelib & HXPKG
.haxelib/*
hmm.json

### Mac
.DS_Store
.icloud

### VS Code
vscode-project.hxml
export/*
.vscode/*
*.code-workspace
dump/*

# Local history which shouldn't be shared.
.history
.ionide

# ! means a file should be added regardless of it it gets ignored prior.
# Including to allow others to use already set-up configuration tweaked for the project.
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
/content
!content/NMV-Base-Game

# Claude Code worktrees
.claude/worktrees/

# Native crash symbol tables -- generated per-build by
# .github/scripts/extract-symbol-table.sh (addresses only match the exact
# build they came from), never source.
/assets/embeds/data/symbols-*.txt
